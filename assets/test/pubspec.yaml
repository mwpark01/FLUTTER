name: sample_app
description: A new Flutter project

publish_to: 'none' # pun.dev에 배포하지 않도록 설정

version: 0.0.0+1

environment:
  # 다트 언어 버전
  sdk: ^3.6.2

# 의존성 명시
dependencies:
  flutter:
    sdk: flutter

  # 애플 아이콘 사용을 위한 패키지
  cupertino_icons: ^1.0.8

# 개발용 의존성 명시
dev_dependencies:
  flutter_test:
    sdk: flutter